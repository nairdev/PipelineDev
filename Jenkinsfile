pipeline {

agent any

stages {
	stage ("checkout") {
		steps { echo "In the checkout stage" }
		}	
	stage ("build") {
		steps { echo "In the build stage" }
		}	
	stage ("release") {
		steps { echo "In the release stage" }
		}
	stage ("test") {
		steps { echo "In the test stage" }
		}	
	stage ("deployment") {
		steps { echo "In the deployment stage" }
		}	
	stage ("cleanup") {
		steps { echo "In the cleanup stage" }
		}
	}
}
